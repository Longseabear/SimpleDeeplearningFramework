defaults:
  - data: default
  - model: basic_denoiser
  - train: default
  - _self_

seed: 42
project_name: deep_learning_framework
run_name: baseline
paths:
  data_root: datasets
  metadata_csv: data/processed/metadata.csv
  tensorboard_root: logs
  checkpoints_dirname: checkpoints
